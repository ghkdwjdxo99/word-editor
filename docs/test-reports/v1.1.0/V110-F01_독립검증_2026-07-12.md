# v1.1.0 V110-F01 찾기 독립 검증

- 검증일: 2026-07-12
- 대상 버전/기능: v1.1.0 / V110-F01 찾기
- 기준 커밋: `9b3d7c6f234ae3303cec7527eaea8fcfe9ee8ea3` 기반 작업 트리
- 검증 방식: Debug/Release 독립 빌드, 전체 콘솔 회귀, Release EXE 직접 실행, Release WPF UI 하네스
- 제품 코드 수정: 없음
- 최종 판정: **통과**

## 1. 빌드 및 실행

| 항목 | 결과 |
|---|---|
| Debug 빌드 | 통과 — 경고 0, 오류 0 |
| Release 빌드 | 통과 — 경고 0, 오류 0 |
| Release EXE 직접 실행 | 통과 |
| 실행 경로 | `SimpleWordEditor/bin/Release/net10.0-windows/SimpleWordEditor.exe` |
| 프로세스 상태 | 시작 성공, 응답 정상 |
| 주 창 제목 | `제목 없음 - 간단 워드 편집기` |

## 2. 자동 테스트

Debug와 Release에서 각각 51건 통과, 0건 실패했다. 기존 v1.0.0 회귀 39건과 V110-F01 신규 12건이 모두 포함됐다.

| ID | 검증 항목 | 결과 |
|---|---|---|
| F01-A01 | 한글 검색 | 통과 |
| F01-A02 | 영문·숫자 검색 | 통과 |
| F01-A03 | 공백 포함 검색 | 통과 |
| F01-A04 | 검색 결과 없음 | 통과 |
| F01-A05 | 빈 검색어 | 통과 |
| F01-A06 | 대소문자 구분 켜기/끄기 | 통과 |
| F01-A07 | 다음 결과 이동 | 통과 |
| F01-A08 | 다음/이전 앞뒤 순환 | 통과 |
| F01-A09 | 중첩 Inline 표시 본문과 문단 경계 | 통과 |
| F01-A10 | 검색어가 문단 경계를 가로지르지 않음 | 통과 |
| F01-A11 | 검색 결과의 WPF 실제 선택 | 통과 |
| F01-A12 | 검색 후 본문 및 변경 상태 유지 | 통과 |

## 3. Release 실제 UI 검증

본문을 `찾기 Search 찾기 123`으로 구성하고 `Search`를 미리 선택한 상태에서 검증했다.

| ID | 시나리오 | 실제 결과 | 판정 |
|---|---|---|---|
| F01-U01 | Ctrl+F | 찾기 패널 표시 | 통과 |
| F01-U02 | 한글 `찾기` 검색 | 첫 결과 선택, 결과 수 표시 | 통과 |
| F01-U03 | 다음 결과 | 두 번째 `찾기`로 이동 | 통과 |
| F01-U04 | 이전 결과 | 첫 번째 `찾기`로 복귀, 상태 `1/2` | 통과 |
| F01-U05 | 찾기 패널 닫기 | 패널 닫힘 | 통과 |
| F01-U06 | 기존 선택 복원 | 닫기 후 `Search` 선택 복원 | 통과 |
| F01-U07 | 본문 유지 | `찾기 Search 찾기 123` 유지 | 통과 |
| F01-U08 | 변경 상태 유지 | 검색과 닫기 전후 `IsDirty=False` | 통과 |

영문·숫자, 대소문자 구분, 결과 없음, 빈 검색어와 앞뒤 순환의 문자열 정책은 UI와 분리된 동일 `FindEngine`을 통해 자동 검증했다. UI 하네스에서는 패널 바인딩, 실제 선택 이동, 상태 표시와 상태 보존을 별도로 확인했다.

## 4. 결함

신규 결함 없음.

## 5. 남은 제한

- Table/List/Section은 저장 차단 대상이며 검색 스냅샷에도 포함되지 않는다.
- 정규식, 전체 결과 동시 강조, 여러 문서 검색은 V110-F01 범위 밖이다.
- IME 조합 중 실시간 검색과 고배율 화면 배치는 별도 사용성 검증 대상이다.

## 6. 판정

**통과.** 요청된 빌드, 기존·신규 회귀, Release EXE, Ctrl+F, 한글·영문·숫자, 대소문자 구분, 다음/이전, 결과 없음, 빈 검색어, 본문 및 변경 상태 보존이 모두 검증됐다. V110-F01은 독립 테스터 승인 상태로 변경할 수 있다.
