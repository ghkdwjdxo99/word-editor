# 간단 워드 편집기 v1.0.0 테스트 결과

## 테스트 개요

- 테스트 일자: 2026-07-12
- 대상 패치: V100-P01~P03, V100-P06~P09
- 환경: Windows, .NET SDK 10.0.301, `net10.0-windows`
- 방식: Debug/Release 빌드, STA 스레드의 실제 WPF `RichTextBox` 변환, DOCX 및 안전 저장 회귀 테스트
- 외부 NuGet 문서 처리 패키지: 추가하지 않음

## 결과 요약

| 구분 | 결과 |
|---|---:|
| Debug 빌드 | 성공, 경고 0, 오류 0 |
| Release 빌드 | 성공, 경고 0, 오류 0 |
| Release 앱 기동 | 성공, 응답 정상 |
| 자동 회귀 테스트 | 37건 통과, 1건 실패 |
| V100-P01 | 개발 완료 |
| V100-P02 | 조건부 실패 — 저장은 차단되나 사용자 안내 결함 발견 |
| V100-P03 | 개발 완료 |
| V100-P06 | 개발 완료 |
| V100-P07 | 개발 완료 |
| V100-P08 | 개발 완료 |
| V100-P09 | 개발 완료 |
| Microsoft Word/LibreOffice 호환성 | 이번 범위 아님, 미검증 |
| Word COM DOC 왕복 | 이번 범위 아님, 미검증 |

## 신규 회귀 테스트

| 패치 | 검증 항목 | 결과 |
|---|---|---|
| V100-P01 | 3단계 중첩 Span의 본문 보존 | 통과 |
| V100-P01 | Bold/Italic/Underline 및 부모 글자 크기 상속 | 통과 |
| V100-P01 | Hyperlink의 표시 문자열 보존 | 통과 |
| V100-P03 | 내부 모델→RichTextBox→내부 모델 본문 왕복 | 통과 |
| V100-P03 | 내부 모델→RichTextBox→내부 모델 지원 서식 왕복 | 통과 |
| V100-P02 | Section 감지와 저장 차단 | 통과 |
| V100-P02 | List 감지와 저장 차단 | 통과 |
| V100-P02 | Table 감지와 저장 차단 | 통과 |
| V100-P06 | 확장자 없음, 잘못된 확장자 확인 및 교정 | 통과 |
| V100-P06 | Word 미설치 DOC 저장 차단과 확장자 대소문자 | 통과 |
| V100-P07 | 새 문서, 편집 후, 저장 후 창 제목 정책 | 통과 |
| V100-P08 | 단일 선택의 굵게·밑줄·크기·정렬 상태 | 통과 |
| V100-P08 | 서로 다른 서식과 정렬의 혼합 상태 | 통과 |
| V100-P09 | 감싼 권한 예외 분류 | 통과 |
| V100-P09 | 파일 점유와 디스크 공간 부족 분류 | 통과 |
| V100-P09 | 손상 문서와 Word 연동 실패 분류 | 통과 |
| V100-P02/P09 | 비지원 편집 블록 사용자 안내 | **실패** |

기존 DOCX 생성, 필수 패키지, 문단/빈 문단, 정렬, 한글/탭/줄바꿈, 지원 서식, 글자 크기 경계, 비지원 DOCX 요소 감지, 안전 저장, 손상/없는 파일 테스트도 모두 통과했다.

## 구현 판정

- 화면에 표시되는 중첩 인라인 본문과 지원 서식은 모델 변환에서 더 이상 조용히 누락되지 않는다.
- 표·목록·Section은 현재 DOCX 모델이 표현할 수 없으므로 저장 전에 차단한다. 다만 사용자 메시지가 차단 원인을 설명하지 못한다.
- 비지원 블록이 있을 때 파일 쓰기와 안전 저장을 시작하지 않으므로 기존 원본은 변경되지 않는다.
- P01~P03 범위의 알려진 데이터 유실 회귀는 발견되지 않았다.

## 확인된 결함

### BUG-V100-001 — 비지원 편집 블록 저장 차단 시 잘못된 사용자 안내

- 심각도: 보통
- 관련 요구사항: V100-P02, V100-P09
- 발생 조건: 편집기에 `Table`, `List` 또는 `Section`이 포함된 상태에서 저장한다.
- 기대 결과: 저장을 중단하고 비지원 블록 종류와 일반 텍스트로 다시 붙여넣는 등의 해결 방법을 안내한다.
- 실제 결과: 저장은 안전하게 중단되지만 `UnsupportedEditorContentException`이 `UserErrorClassifier`에서 분류되지 않아 `Unknown`으로 처리된다. 사용자에게는 `다시 시도하거나 다른 파일을 선택해 주세요.`가 표시된다.
- 재현 결과: `Kind=Unknown`, `Action=다시 시도하거나 다른 파일을 선택해 주세요.`
- 영향: 데이터나 기존 파일은 손상되지 않지만, 사용자는 저장 실패 원인과 해결 방법을 알 수 없다.
- 원인: `UserErrorClassifier.Classify`에 `UnsupportedEditorContentException` 분기가 없다.
- 권장 수정: 해당 예외를 `UserErrorKind.Unsupported`로 분류하고 `BlockTypes`를 포함한 해결 안내를 반환한다.
- 회귀 테스트: `V100-P02 비지원 편집 블록 사용자 안내`를 추가했으며 현재 실패한다.

## 남은 위험

1. 브라우저, Word 등 외부 프로그램별 실제 클립보드 데이터 형식은 수동 붙여넣기 시험이 더 필요하다.
2. 비지원 블록은 텍스트 평탄화가 아니라 저장 차단 방식이므로 사용자가 일반 텍스트로 다시 붙여넣어야 한다.
3. Microsoft Word와 LibreOffice의 실제 DOCX 호환성은 V100-P04에서 검증해야 한다.
4. DOC COM 왕복과 `WINWORD.EXE`/임시 파일 정리는 V100-P05에서 검증해야 한다.
5. 실제 대화상자와 도구 모음을 마우스·키보드로 조작하는 수동 UI 검증은 테스터 단계에서 추가로 필요하다.
6. 현재 환경에는 Microsoft Word와 LibreOffice가 모두 설치되어 있지 않아 P04/P05는 실행할 수 없었다.

## 현재 판정

**조건부 실패 / 배포 보류.** 핵심 데이터 유실 방지와 저장 안정성 회귀는 통과했으나 BUG-V100-001이 발견됐다. 이 결함 수정 후 38건 전체 재실행이 필요하다. 또한 v1.0.0 전체 배포 승인은 P04, P05, P10 및 실제 UI·오피스 환경 검증 전까지 보류한다.
